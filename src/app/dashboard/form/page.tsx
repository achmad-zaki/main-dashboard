import { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Form',
}

export default function FormPage() {
    return (
        <section>
            <h1>form page</h1>
        </section>
    )
}
